(defvar mu4e-compose-context-policy 'ask-if-none)

(defvar mu4e-context-policy 'pick-first)

(defvar mu4e-enable-mode-line t)

(defvar mu4e-enable-notifications t)

(defvar mu4e-maildir (expand-file-name "~/.mail"))

(defvar mu4e-view-show-images t)
